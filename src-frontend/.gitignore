node_modules/
dist/
.vite/
*.local
.eslintcache

# routeTree.gen.ts (TanStack Router) is committed on purpose so `tsc`/CI work on
# a clean checkout where nothing has run the Vite plugin yet. The plugin keeps it
# up to date on every dev run and build.
