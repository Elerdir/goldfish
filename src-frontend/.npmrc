# Don't let pnpm auto-install before running a script. Tauri's beforeBuildCommand
# (`pnpm build`) assumes deps are already installed (`pnpm install`), so the build
# never triggers a slow/fragile mid-build install on a constrained registry mirror
# — it just runs tsc + vite against the installed node_modules.
verify-deps-before-run=false
